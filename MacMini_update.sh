brew update && brew upgrade && brew cleanup && brew doctor && echo Brew done Mac mini || echo Error
brew update && brew upgrade && brew cleanup && brew doctor && echo Brew done Mac mini || echo Error
