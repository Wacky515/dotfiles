snippet temp
    #!/bin/bash
    # @(#) ${1:NAME}
    # Created:     ${2:DATE}
    # Last Change: 2018/06/03 12:40:21.

    for f in ~/dotfiles/function/*.sh
    do
        source ${f}
    done

    readonly PROCESS="${3:ECHO}"

    gm_echo ">> ${PROCESS^}"
    # ym_echo ">> Start "

    ${4:START}

    result_echo $? "${PROCESS}"
