snippet if
    if ${1:CONDITION} (
    ${2:TRUE}
    ) else if (
    ${3:ELIF}
    ) else (
    ${4:FALSE}
    )

snippet ifnot
    if not ${1:CONDITION} (
    ${2:TRUE}
    ) else if (
    ${3:ELIF}
    ) else (
    ${4:FALSE}
    )
